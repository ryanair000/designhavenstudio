'use client'
import { useState } from 'react'
import { Icon } from './icons'
export function ContactForm({packages=[]}){
 const [state,setState]=useState({loading:false,error:'',success:false})
 async function submit(e){e.preventDefault();setState({loading:true,error:'',success:false});const form=new FormData(e.currentTarget);const body=Object.fromEntries(form.entries());try{const r=await fetch('/api/enquiries',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)});const d=await r.json();if(!r.ok)throw new Error(d.error||'Could not send request');e.currentTarget.reset();setState({loading:false,error:'',success:true})}catch(err){setState({loading:false,error:err.message,success:false})}}
 return <form className="contact-form" onSubmit={submit}><div className="form-grid"><label>Full name<input name="name" required minLength={2} placeholder="Your name"/></label><label>Business / brand<input name="business" placeholder="Business name"/></label><label>Email address<input name="email" type="email" required placeholder="you@example.com"/></label><label>Phone / WhatsApp<input name="phone" placeholder="+254..."/></label><label>Design category<select name="category"><option>Business promotions</option><option>Brands & Products</option><option>Events & Entertainment</option><option>Food & Hospitality</option><option>Corporate</option><option>Personal</option></select></label><label>Preferred package<select name="package"><option value="">Not sure yet</option>{packages.map(p=><option key={p.id||p.name}>{p.name}</option>)}</select></label><label>Preferred deadline<input type="date" name="deadline"/></label><label className="honeypot" aria-hidden="true">Website<input name="website" tabIndex={-1} autoComplete="off"/></label></div><label>Tell us about the poster<textarea name="description" required minLength={10} rows={6} placeholder="What are you promoting? What text, images and brand details should be included?"/></label>{state.error&&<p className="form-error">{state.error}</p>}{state.success&&<p className="form-success"><Icon name="check" size={18}/> Request received. We will contact you with the next steps.</p>}<button className="button button-primary button-large" disabled={state.loading}>{state.loading?'Sending...':'Send project request'} <Icon name="arrow" size={18}/></button></form>
}
