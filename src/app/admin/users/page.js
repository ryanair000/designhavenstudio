import { ProtectedPage } from '@/components/admin/protected-page'
import { getAdmin } from '@/lib/admin'
export default async function Page(){const admin=await getAdmin();return <ProtectedPage title="Users & security" subtitle="Review account access and session security"><div className="admin-content"><div className="admin-page-head"><div><h2>Account security</h2><p>The dashboard uses private, expiring HTTP-only sessions.</p></div></div><div className="admin-two-col"><section className="admin-card"><h3>Owner account</h3><p><strong>{admin?.fullName||admin?.full_name}</strong><br/>{admin?.email}<br/><span className="badge success">{admin?.role||'owner'}</span></p></section><section className="admin-card"><h3>Security controls</h3><p>Passwords are securely hashed. Session tokens are stored only as hashes and expire after seven days.</p></section></div></div></ProtectedPage>}
